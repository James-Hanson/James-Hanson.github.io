\begin{frame}
  \frametitle{Bounded $k$-splitting}

  \begin{tikzpicture}[remember picture,overlay]
    \node[text width=10.5cm,align=center,font=\normalsize] at
      (current page.center)
      {$T$ has {\color{cyan}bounded $k$-splitting} if there is a $\kappa$
      such that for any (partitioned) type
      $p(x_0;x_1;\dots;x_{k-1})\in S(B)$, there is a $C\subseteq B$ with
      $|C|\leq\kappa$ such that $p$ does not $k$-split over $C$};
  \end{tikzpicture}
\end{frame}
